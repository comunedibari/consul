<% if user_signed_in? %>
  <li id="notifications">
      
      <% if current_user.notifications.unread.count > 0 %>
          <button class="btn btn-default btn-lg btn-link" style="color: white;" 
              data-toggle="tooltip" data-placement="bottom" title="<%= t('layouts.header.notification_item.new_notifications', 
              count: current_user.notifications.unread.count).html_safe %>" onclick="location.href='<%= notifications_path %>'">
            <i class="fa fa-bell"></i>
          </button>
          <span class="badge badge-notify"><%= current_user.notifications.unread.count %></span>
      <% else %>
          <button class="btn btn-default btn-lg btn-link" style="color: white;"
              data-toggle="tooltip" data-placement="bottom" title="<%= t('layouts.header.notification_item.no_notifications') %>"
              onclick="location.href='<%= notifications_path %>'">
            <i class="fa fa-bell-slash"></i>
          </button>
      <% end %>

  </li>

<% end %>