
<% #calcola array marker
geolocation_arr = []
    @debates.each do |debate|
        if  debate.map_location
            geolocation = debate.map_location
            geolocation_arr.push(geolocation) 
        end 
    end
%>

<% provide :title do %><%= t('debates.index.title') %><% end %>

<% if User.pon_id > 0 %>



<main>
   
<div class="help-header no-margin-top margin-bottom">
  <div class="row">
    <div class="small-12 column" data-magellan>
      <%= image_tag "#{Rails.application.config.cm}/help/help_icon_debates.png", alt: t("debates.form.geolocation"), class: "align-top" %>
      <h1 class="inline-block"><%= t("debates.form.geolocation") %></h1>
      
    </div>
  </div>
</div>

  <div class="row">
    <div class="col-12 mb-3">
      <p><%= back_link_to %></p>
    </div>
    <p><%= t("#{controller_name}.map.info") %></p>
    <p><%= t("#{controller_name}.map.count_markers") %> <%= count_markers = item_with_maplocation_count(@debates) %></p>


<p><%= render_geolocation(@debates, "debate","debate_all")  %></p>

       

  </div>
</main>

  <% else%>
    <main>
      <div class="row">
      </div>
    </main>
  <% end%>









