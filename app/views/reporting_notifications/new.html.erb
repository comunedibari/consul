<div class="row">
  <div class="small-12 column">
    <%= back_link_to %>

    <h1><%= t("reporting_notifications.new.title") %></h1>

    <div class="callout primary">
      <p>
        <%= t("reporting_notifications.new.info_about_receivers_html",
              count: @reporting.users_to_notify.count,
              reporting_page: link_to(t("reporting_notifications.new.reporting_page"),
                                     reporting_path(@reporting, anchor: "comments"))).html_safe %>
      </p>
    </div>
  </div>
</div>

<div class="row">
  <div class="small-12 medium-9 column">
    <%= form_for @notification do |f| %>
      <%= render "shared/errors", resource: @notification %>

      <%= f.label :title, t("reporting_notifications.new.title_label") %>
      <%= f.text_field :title, label: false %>

      <%= f.label :body, t("reporting_notifications.new.body_label") %>
      <%= f.text_area :body, label: false, rows: "3" %>

      <%= f.hidden_field :reporting_id, value: @reporting.id %>

      <div class="small-12 medium-4">
        <%= f.submit t("reporting_notifications.new.submit_button"), class: "btn btn-primary btn-block" %>
      </div>
    <% end %>
  </div>
</div>
